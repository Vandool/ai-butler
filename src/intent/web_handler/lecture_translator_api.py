from src.intent.processable import Processable


class LectureTranslatorApi(Processable):
    def process(self, the_input: str) -> str:
        # TODO(Arvand): Classify the_input, map to one of the functions
        # return the result
        ...
